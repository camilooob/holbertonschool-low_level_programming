#include "holberton.h"
#include <stdio.h>

/**
 * print_array - a function that prints n elements of an array
 * of integers, followed by a new line.
 * @a: input pointer of an array
 * @n: input array
 */

void print_array(int *a, int n)
{
	int numeros;

	if (> 0)
		for (numeros = 0; numeros < n; numeros++)
		{
			printf("%d", a[numeros]);
			if (i < numeros - 1)
				printf(", ");
		}
	printf("\n");
}
