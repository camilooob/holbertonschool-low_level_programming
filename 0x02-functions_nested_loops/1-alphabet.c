#include "holberton.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
char letra;
for (letra = 'a'; letra <= 'z'; letra++)
_putchar(letra);
_putchar('\n');
}
